# Review — personal-landing — 2026-09-07

**Reviewer:** independent clean-context pass (`sdd:reviewer`, model opus) + author remediation.
**Scope:** `git diff 702b335..HEAD` — the whole feature branch.
Final state after remediation: **61 files, +5199 / −338**; feature commits `bcb60e8..7a623a8`.

## Verdict: PASS

Initial pass returned **CHANGES REQUESTED** — one shipped AC (AC-08) was factually
violated at the reference phone viewport and the test that claimed to guard it could
not fail. All stage-1 findings and the stage-2 findings are now resolved; one nit is
deferred as a standing launch-checklist item.

## Method

The reviewer re-read `spec.md` (§4/§5/§6/§6.1/Test plan), `sad.md`, `data-model.md`,
ADR-0005–0008, `screens.md`, `ux-flows.md`, `tasks.json`, and traced every non-withdrawn
US/AC into code + test. It ran `npm run check` (clean), `npm test` (80/80 at the time),
`npx playwright test --grep-invert @visual` (23 passed), and **measured the built `dist/`
in a real browser** at 1280×800 / 390×844 / 360×800. Remediation was re-verified the same
way (browser measurement of the fold + full gate).

## Stage 1 — AC / story trace

Every non-withdrawn user story (US-01, US-02, US-05, US-06, US-07, US-08, US-10, US-11)
has ≥ 1 AC and reaches code + test. Withdrawn: AC-03 / AC-09 / AC-11 / AC-12 (no v1
behaviour, no test — correct). AC-01 at 1280×800, AC-02, AC-04, AC-05, AC-06, AC-07,
AC-10, AC-13, AC-14, AC-15 verified genuinely satisfied across the declared
`web-frontend` surface (component + e2e-through-UI, not only build/schema).

| # | Finding | Severity | Resolution |
|---|---|---|---|
| 1 | **AC-08 / AC-01 violated at 390×844** — the top-stack (an AC-01 essential) rendered ~258px below the fold. The §6 drop order fired only `@media (max-width: 359.98px)`, where the fold guarantee is not binding, never at 390px where it is. `Hero.astro`. | blocker | **Fixed** — `245a7a9`. Drop order moved to the phone breakpoint (`max-width: 767.98px`); top-stack bottom 1102px → 834px. Pinning test `landing.spec.ts` "every above-the-fold essential sits within the fold" (RED `1102 > 844` → GREEN). `_fixes/2026-09-07-phone-fold-top-stack.md`. AC-15 note amended so it doesn't read as contradicting the drop order. |
| 2 | **The AC-01/AC-08 e2e test asserted nothing about the fold** — `toBeVisible()` is true for an element below the viewport; the top stack was only `toHaveCount(8)`. `landing.spec.ts`. | blocker | **Fixed** — `245a7a9`. New test asserts `box.y + box.height <= viewport height` per essential, at both projects. |
| 6 | **AC-05 canonical-essentials coverage partial** — `name`, headshot `src`/`alt`, `availability.status`, `contact.location` had no fixture (schema enforced them; no test). | minor | **Fixed** — `7a623a8`. `missingEssentialProfile()` + 5 unit rows. |
| 7 | **E15 (no `mailto:` handler → no copy affordance) had no test.** Correct by construction. | minor | **Fixed** — `7a623a8`. `header.test.ts` asserts no `<button>` / clipboard control / address outside the `href`. |

## Stage 2 — quality

| # | Finding | Severity | Resolution |
|---|---|---|---|
| 3 | **`buildFixture` cache masked failures** — parallel `astro build`s shared one Astro content-layer store (symlinked `node_modules/.astro`), leaking fixture `profile` content into `buildFixture({})`; cache key ignored `src/`. A cold-cache run showed 2 real failures on the committed HEAD. | major | **Fixed** — `245a7a9`. Per-fixture `cacheDir`; cache key hashes the `src/` tree. Gate now deterministic (verified cold ×2 + warm). |
| 4 | **Browser test tier unwired from CI**, against spec §Test plan "still gating"; the removal mis-cited ADR-0008 (which only dropped Chromium for the CV-PDF postbuild). | major | **Fixed** — `7a623a8`. `ci.yml` installs Chromium + runs `npm run test:e2e` after the build. Visual-regression stays local until Linux baselines exist (`launch-checklist.md`). `workflows.test.ts` updated. |
| 5 | **`Footer.astro` hard-coded the surname + year**, frozen by `layout.test.ts` — undercuts the anti-drift purpose (US-07, SAD §8). | major | **Fixed** — `7a623a8`. `owner` from `profile.name` via Layout (required prop, no name in the shell); year = build year. |
| 8 | **Top-stack chips rendered at 14.4px** vs spec §6 "body text ≥ 16px"; they carry an AC-01 essential. Industries note + header label also < 16px. | minor | **Fixed** — `7a623a8`. Chips / Industries note / header action label → 1rem. Phone stack gap tightened so 16px chips still clear the fold; e2e asserts chip font size. |
| 9 | **E14 over-claimed** — titled "…recruiter-link-label" but never asserted a `/t/{label}` token; scoped to `index.html` without saying so. | minor | **Fixed** — `7a623a8`. Adds `/t/{label}` / `utm_` / `recruiter-link` token assertions + a scope comment (the committed CV PDF in `dist/` deliberately carries contact details, AC-07). |
| 10 | `aria-label` on a role-less `<div>` (`Hero.astro` top-stack). | nit | **Fixed** — `7a623a8`. Now `<section aria-label>`, consistent with `AvailabilityBlock` / `Industries`. |
| 11 | `scroll-behavior: smooth` with no `prefers-reduced-motion` opt-out. | nit | **Fixed** — `7a623a8`. Guarded by `@media (prefers-reduced-motion: no-preference)`. |
| 12 | Committed CV PDF is 3.6 MB; page↔CV parity could not be verified on the review machine (no `pdftotext`/`mutool`/`qpdf`). Not an AC breach — spec §6 budget is *initial page weight*, ADR-0008 accepts the tracked binary. | nit | **Deferred** — `launch-checklist.md` "Page ↔ committed CV parity" row stays unchecked; owner Roman, ongoing from first launch. Revisit trigger: CV generated from the profile content (roadmap step 4). |
| — | **US-11 has no `sad.md` §6 flow** (covered in `ux-flows.md` + SCR-01). | doc | **Fixed** — `7a623a8`. One-line §6 note (static hero content, no runtime flow). |

## Addendum — 2026-09-07, post-review polish pass

After this PASS, Roman ran a header/footer polish pass (taller header, name = hero-name size,
matching section rule, `Copyright ©` in the footer) and asked for the reference resume header's
scroll-driven active-section indicator. That indicator needs a script, which the "Checked and
clean" item below no longer describes accurately:

- **The 0 KB client-JS property is relaxed by [ADR-0009](../adr/0009-scroll-spy-active-section-indicator-progressive-enhancement-script.md).**
  One ~320 B inline `IntersectionObserver` module now ships (scroll-spy active-section
  indicator). Still **no JS bundle, no framework island, no hydration**, and a strict
  progressive enhancement — JS off ⇒ no highlight, nothing else changes.
- `index-page.build.test.ts` + `header.test.ts` updated to assert exactly that shape
  (one inline `type="module"` script < 1 KB, containing `IntersectionObserver`; still
  `js === []` and no `astro-island`).
- `spec.md` §6 NFR + NFR-verification row, `sad.md` §1/§2/§8/§9/§10 amended.

## Checked and clean (no action)

- Zero client JS — genuinely enforced over the real `dist/` (`index-page.build.test.ts`).
  **See the addendum above:** relaxed to ≤ 1 KB / one inline PE script by ADR-0009.
- AC-07 / AC-10 exposure — email/LinkedIn only in `href`; asserted over `dist/` and per control.
- AC-13 — `target="_blank" rel="noopener"` + `download`, verified through a real browser download event.
- AC-04 — one shared filename helper; unit edge rows + present/absent/0-byte/renamed integration.
- AC-05 / AC-06 — enforced by the real Astro pipeline, each edge (E1–E13) names the field.
- AC-14 / AC-15 — content-driven, both `industries` paths built, phone reflow verified in-browser.
- AC-01 at 1280×800 — measured clean.
- `astro.config.mjs` `site`/`base` deferral — acceptable; hrefs use `import.meta.env.BASE_URL`, nothing emits an absolute URL. Launch-checklist gate, not a merge gate.

## Gate (final)

`npm run check` 0 errors / 0 warnings / 0 hints · `npm test` 88/88 (cold cache) ·
`npm run test:e2e` 25 passed / 3 skipped · `npm run test:visual` 5 passed.

## Follow-ups carried forward

- §6 drop-order steps 2 (drop Tagline on phone) and 3 (top-stack truncation) are still not
  implemented — not needed for the committed content, but a ~300-char `tagline` could
  re-overflow 390×844. Owner: Roman, revisit if a longer tagline lands.
- At 360px the hero is ~51px over the fold even with Industries dropped — within spec (§6:
  only no-horizontal-scroll binds there).
- `screens.pen` has an unrelated uncommitted change (Pencil app auto-save) — left for Roman.
